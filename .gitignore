.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
*.pyc
.build/
.cache/

# Elgato packaging build artifacts
build/

# Brainstorming visual companion (mockups, session state)
.superpowers/

# herdr-agent-company run dirs (manifest, inbox, evidence)
.scratch/

# Local coding-agent state (Claude Code settings, worktrees, scheduled tasks)
.claude/

# Internal design/planning docs (SDD plans + specs) — kept locally, not published
docs/superpowers/

# local tooling artifacts (roborev config, clawpatch cache, uv lockfile) — not part of the project
.clawpatch/
.roborev.toml
uv.lock

# npm only belongs under desktop/, but running a JS tool from the repo root
# creates one here too — and only desktop/.gitignore was guarding against it.
node_modules/
