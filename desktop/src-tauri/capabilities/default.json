{
  "identifier": "default",
  "description": "Default capability for both role windows, the deck and the app (core APIs: events for discovery, window controls).",
  "windows": ["main", "config"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-start-dragging"
  ]
}
